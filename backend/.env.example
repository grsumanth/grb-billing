# ── Server ──────────────────────────────
PORT=5000

# ── Supabase PostgreSQL ──────────────────
DB_HOST=your-supabase-db-host.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your-database-password
DATABASE_URL=postgresql://postgres:your-database-password@your-supabase-db-host.supabase.co:5432/postgres

# ── Supabase API (Storage & Storage Auth) ────────────────
SUPABASE_URL=https://your-supabase-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key-backend-only

# ── Authentication & Security ────────────
JWT_SECRET=your-secure-jwt-secret-string
JWT_EXPIRES_IN=7d

# ── SMTP Email configurations ────────────
GMAIL_USER=your-app-email@gmail.com
GMAIL_PASS=your-gmail-app-password

# ── Google Drive backup configurations ────────────
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account-email@project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# Optional (but highly recommended to avoid "Service Accounts do not have storage quota" error):
# Create a folder in your personal Google Drive, share it with the service account email above as Editor,
# and paste its folder ID below.
GOOGLE_DRIVE_ROOT_FOLDER_ID=your-google-drive-shared-folder-id


